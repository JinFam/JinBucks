// js/sync-config.js — JinBucks 동기화 설정 (비밀 없음, 커밋 안전).
// 실제 레시피 데이터는 아래 비공개 레포에 저장되며, 각자 입력한 PAT로만 접근된다.
export const syncConfig = {
  owner: 'JinFam',
  repo: 'JinBucks-sync',
  branch: 'main',
  dir: 'state',
  pollMs: 5000,
  label: '',
};
